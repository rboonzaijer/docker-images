name: Build and Publish Docker Containers

on:
  schedule:
    - cron: '45 1 * * *' # https://crontab.guru/#45_1_*_*_* “At 01:45.”
    - cron: '45 13 * * *' # https://crontab.guru/#45_13_*_*_* “At 13:45.”
  push:
    branches:
      - main
  workflow_dispatch:

jobs:

#######################################################################################

  alpine_latest:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u freshcontainer --password-stdin
          docker build --no-cache --pull -f Dockerfile.alpine.latest -t freshcontainer/alpine:latest .
          docker push freshcontainer/alpine:latest
      - uses: aquasecurity/trivy-action@master
        with:
          image-ref: freshcontainer/alpine:latest
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL'
      - uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'

#######################################################################################

  curl_latest:
    needs: alpine_latest
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u freshcontainer --password-stdin
          docker build --no-cache --pull -f Dockerfile.curl.latest -t freshcontainer/curl:latest .
          docker push freshcontainer/curl:latest
      - uses: aquasecurity/trivy-action@master
        with:
          image-ref: freshcontainer/curl:latest
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL'
      - uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'

#######################################################################################

  imagemagick_latest:
    needs: alpine_latest
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u freshcontainer --password-stdin
          docker build --no-cache --pull -f Dockerfile.imagemagick.latest -t freshcontainer/imagemagick:latest .
          docker push freshcontainer/imagemagick:latest
      - uses: aquasecurity/trivy-action@master
        with:
          image-ref: freshcontainer/imagemagick:latest
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL'
      - uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'

#######################################################################################

  nginx_latest:
    needs: alpine_latest
    permissions:
      security-events: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u freshcontainer --password-stdin
          docker build --no-cache --pull -f Dockerfile.nginx.latest -t freshcontainer/nginx:latest .
          docker push freshcontainer/nginx:latest
      - uses: aquasecurity/trivy-action@master
        with:
          image-ref: freshcontainer/nginx:latest
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL'
      - uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'

#######################################################################################

  php_nginx_83:
    needs: nginx_latest
    permissions:
      security-events: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u freshcontainer --password-stdin
          docker build --no-cache --pull -f Dockerfile.php-nginx.8.3 -t freshcontainer/php-nginx:8.3 .
          docker push freshcontainer/php-nginx:8.3

          docker tag freshcontainer/php-nginx:8.3 freshcontainer/freshcontainer:php-nginx-8.3
          docker push freshcontainer/freshcontainer:php-nginx-8.3
      - uses: aquasecurity/trivy-action@master
        with:
          image-ref: freshcontainer/php-nginx:8.3
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL'
      - uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'

#######################################################################################

  php_nginx_83_dev:
    needs: php_nginx_83
    permissions:
      security-events: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u freshcontainer --password-stdin
          docker build --no-cache --pull -f Dockerfile.php-nginx.8.3-dev -t freshcontainer/php-nginx:8.3-dev .
          docker push freshcontainer/php-nginx:8.3-dev
  
          docker tag freshcontainer/php-nginx:8.3-dev freshcontainer/freshcontainer:php-nginx-8.3-dev
          docker push freshcontainer/freshcontainer:php-nginx-8.3-dev
      - uses: aquasecurity/trivy-action@master
        with:
          image-ref: freshcontainer/php-nginx:8.3-dev
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL'
      - uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'

#######################################################################################
